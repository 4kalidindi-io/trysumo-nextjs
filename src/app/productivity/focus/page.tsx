import FocusApp from '@/components/apps/focus/FocusApp';

export const metadata = {
  title: 'Focus | TrySumo',
  description: 'Plan your day with manual event scheduling and customizable focus sessions to maximize productivity.',
};

export default function FocusPage() {
  return <FocusApp />;
}
